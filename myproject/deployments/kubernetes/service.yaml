apiVersion: v1
kind: Service
metadata:
  name: myproject-server
spec:
  selector:
    app: myproject
    component: server
  ports:
    - name: http
      port: 80
      targetPort: 8080
    - name: grpc
      port: 9090
      targetPort: 9090
  type: ClusterIP