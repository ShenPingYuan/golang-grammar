server:
  port: "8080"
  mode: "development"

grpc:
  port: "9090"

jwt:
  secret: "my-super-secret-key-change-in-production"
  expiry: "24h"

database:
  driver: "memory"
  dsn: "user:password@tcp(localhost:3306)/myproject?parseTime=true"

redis:
  addr: "localhost:6379"
  password: ""
  db: 0

log:
  level: "debug"
  format: "text"

mq:
  driver: "memory"
  brokers:
    - "localhost:9092"